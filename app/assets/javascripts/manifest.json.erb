<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "IchiOshi",
  "short_name": "IchiOshi",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "https://s3-ap-northeast-1.amazonaws.com/tamtim/uploads/IchiOshi_favicon.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#FFFFFF",
  "background_color": "#FFFFFF",
  "display": "standalone",
  "orientation": "portrait"
}
