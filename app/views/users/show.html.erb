<% provide(:title, @user.name) %>
<h1>My page</h1>

<ul class="users">
  <li><%= @user.name %></li>
  <li><%= @user.email %></li>
</ul>

<%= link_to 'ユーザーを削除する', user_destroy_path(id: current_user.id), method: :delete %>