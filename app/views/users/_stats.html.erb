<% @user ||= current_user %>

<div class="stats_first">
    <span class="stat">投稿</span>
    <span class="FollowCount"><%= @user.reviews.count %></span>
    <span class="stat">件</span>
</div>

<div class="stats">
  <span class="stat">
  <a href="<%= followers_user_path(@user) %>">
    <span class="stat">フォロワー</span>
    <span class="FollowCount"><%= @user.followers.count %></span>
    <span class="stat">人</span>
  </a>
  </span>
</div>

<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <span class="FollowCount"><%= @user.followings.count %></span>
    <span class="stat">人をフォロー中</span>
  </a>
</div>
