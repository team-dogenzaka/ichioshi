<%= form_with(model: [review, comment] ) do |form| %>
  <div class="form-group">
    <%= form.text_area :content, class: "form-control" %>
  </div>
  <%= form.submit "コメントをする", class: "btn btn-round btn-d" %>
<% end %>