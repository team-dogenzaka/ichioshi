.col-sm-4.col-md-3.col-md-offset-1.sidebar
  .widget
    form[role="form"]
      .search-box
        input.form-control[type="text" placeholder="Search..."]
        button.search-btn[type="submit"]
          i.fa.fa-search
  .widget
    h5.widget-title.font-alt
      | #プレゼントの人気のレビュー
    ul.icon-list
      - @post_user.reviews.last(5).each do |review|
        li
          = link_to review.title, review_path(review.id)
  .widget
    h5.widget-title.font-alt
      | Takuya Mishimaの人気のレビュー
    ul.icon-list
      - @post_user.reviews.last(5).each do |review|
        li
          = link_to review.title, review_path(review.id)
  .widget
    h5.widget-title.font-alt
      | Tag
    .tags.font-serif
      a[href="#" rel="tag"]
        | Blog
      a[href="#" rel="tag"]
        | Photo
      a[href="#" rel="tag"]
        | Video
      a[href="#" rel="tag"]
        | Image
      a[href="#" rel="tag"]
        | Minimal
      a[href="#" rel="tag"]
        | Post
      a[href="#" rel="tag"]
        | Theme
      a[href="#" rel="tag"]
        | Ideas
      a[href="#" rel="tag"]
        | Tags
      a[href="#" rel="tag"]
        | Bootstrap
      a[href="#" rel="tag"]
        | Popular
      a[href="#" rel="tag"]
        | English
